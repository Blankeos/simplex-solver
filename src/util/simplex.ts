import { Matrix } from "mathjs";

export default function simplex(c: number[], A: Matrix, b: number[]) {
  // tableau
  // Initial tableau

  while (canImprove(1)) {
    // pivot findPivotIndex
    // pivotAbout
  }

  // return optimal tableau, objectiveValue
}

function canImprove(tableau: any) {
  return false;
}
